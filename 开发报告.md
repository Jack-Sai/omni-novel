# Omni Novel 开发报告

> 截止日期：2026-09-13
> 项目代号：Omni Novel
> 技术栈：Tauri v2 + React 19 + TypeScript 6 + Vite 8 + Zustand + TipTap v3 + Radix UI + Tailwind CSS v4

---

## 一、项目概述

Omni Novel 是一款由本地部署大模型驱动的 AI 小说创作软件，目标覆盖从灵感、设定、大纲、章节、正文、修订到导出的全流程。软件完全本地运行，不提供任何云服务，所有数据归用户所有。

当前项目处于 **MVP 早期阶段**，已完成前端架构骨架搭建、AI 集成、UI 组件体系、数据模型定义等核心工作。

---

## 二、已完成功能详细说明

### 2.1 设计系统（已完成）

**实现方式**：
- 在 `src/index.css` 中建立完整的语义化 CSS 变量体系
- 所有全局规则收进 `@layer base` 或 `@layer components`，确保不压掉 Tailwind 工具类
- 通过 `@custom-variant dark (&:where(.dark, .dark *))` 实现暗色模式

**具体实现**：
- **表面层级**：canvas → subtle → surface → elevated → hover（5 级层次），侧栏用 subtle，内容区用 canvas，卡片用 surface
- **描边**：line / line-strong 两级
- **文字**：ink / ink-2 / ink-3 / ink-inverse（3 级灰度）
- **主色**：primary / primary-hover / primary-soft / primary-line / on-primary
- **状态色**：success / warning / danger 各有 3 级（color / soft / line）
- **阴影**：xs / sm / md / lg / xl（5 级）
- **圆角**：xs(6) / sm(8) / md(10) / lg(12) / xl(16) / 2xl(20)
- **字体栈**：系统字体（sans）、衬线字体（serif，用于编辑器）、等宽字体（mono）
- **动画系统**：overlay-in/out、panel-in/out、panel-top-in/out、fade-up，支持 `prefers-reduced-motion`

### 2.2 UI 组件库（已完成 16 个）

**实现方式**：
- 在 `src/components/ui/` 目录下统一管理
- 使用 `clsx` + `tailwind-merge` 实现类名合并（`cn` 工具函数）
- 基于 Radix UI 封装 Dialog、Menu 等复合组件

**组件清单**：
1. `Badge` — 标签组件，支持 primary/success/warning/danger/neutral/outline 6 种 variant
2. `Button` — 按钮组件，支持 primary/secondary/ghost/danger 4 种 variant，sm/md/lg/icon-sm/icon 5 种 size，loading 状态
3. `Card` — 卡片组件，支持 interactive 模式（hover 效果、键盘可达性）和 padded 控制
4. `Dialog` — 基于 Radix UI Dialog，支持 4 种尺寸、center/top 定位、hideHeader、flush 模式、图标
5. `EmptyState` — 空状态组件，支持图标、标题、描述、操作按钮
6. `Input` — 输入框组件，支持 sm/md 两种尺寸
7. `Textarea` — 多行输入，支持 autoResize
8. `Select` — 下拉选择框，自定义样式（圆角、主题色边框、聚焦效果）
9. `Label` — 标签组件
10. `Field` — 表单字段容器，统一标签、说明文字与错误提示的间距
11. `Menu` — 基于 Radix UI DropdownMenu 的下拉菜单
12. `Page` — 页面容器，包含 PageHeader（标题/描述/leading/actions）和 PageBody（center/width/padded）
13. `Progress` — 进度条组件
14. `Section` — 分区组件，支持标题、图标、描述
15. `SegmentedControl` — 分段控制器，支持 segment（内嵌式）和 chip（胶囊式）两种变体，支持计数显示
16. `StatCard` — 统计卡片，支持 icon、tone、align
17. `ThemeSwitcher` — 侧栏主题切换器（亮/暗 + 强调色色板）
18. `ThemeToggle` — 完整主题控制面板（模式 + 强调色 + 色调 + 边框圆角）
19. `WordStats` — 字数统计面板（总字数、本章字数、平均每章、目标进度）
20. `Kbd` — 键盘按键显示组件

### 2.3 主题系统（已完成）

**实现方式**：
- 在 `src/stores/themeStore.ts` 中使用 Zustand + persist 中间件
- 通过 CSS 变量注入实现主题切换，无需重渲染
- 所有设置持久化到 localStorage

**4 维主题定制**：
| 维度 | 选项 | 说明 |
|------|------|------|
| 模式 | light / dark / system | system 模式监听 `prefers-color-scheme` 媒体查询自动切换 |
| 强调色 | blue / violet / rose / emerald / amber / cyan / slate | 7 种，每种都有完整的亮/暗两套色值 |
| 表面底色 | cool / warm / neutral | 3 种，影响 subtle/canvas/surface/elevated/hover 颜色 |
| 边框风格 | default / softer / sharper | 3 种，同时控制圆角大小（softer 更大圆角，sharper 更小圆角） |

**UI 组件**：
- `ThemeSwitcher`：侧栏底部的简洁切换器（亮/暗 + 强调色选择器）
- `ThemeToggle`：设置页的完整主题控制面板

### 2.4 用户系统（已完成）

**实现方式**：
- SQLite 数据库存储用户数据（`src/services/database.ts`）
- Zustand `userStore` 管理用户状态
- localStorage 存储 `currentUserId` 实现登录态持久化

**具体功能**：
- 用户注册：用户名+密码注册，可选手机号/显示名，手机号格式校验（1开头11位），密码最少6位
- 用户登录：用户名密码校验，登录成功后跳转书架页
- 用户登出：清除 localStorage 中的 currentUserId，重置状态
- 会话恢复：页面刷新后自动恢复登录状态

**数据库表结构**（`users` 表）：
```sql
CREATE TABLE users (
  id TEXT PRIMARY KEY,
  username TEXT UNIQUE NOT NULL,
  phone TEXT,
  password TEXT NOT NULL,
  display_name TEXT,
  avatar TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 2.5 项目管理（已完成）

**实现方式**：
- Zustand `projectStore` 管理项目状态
- SQLite `projects` 表持久化项目数据
- 书架页（BookshelfPage）展示所有项目

**具体功能**：
- **书架页**：卡片网格展示项目，显示书名/作者/类型(中文)/字数/更新日期，响应式布局（sm:2列 / lg:3列 / xl:4列）
- **新建项目**：15种小说类型选择（玄幻/奇幻/武侠/仙侠/都市/现实/军事/历史/游戏/体育/悬疑/科幻/言情/轻小说/其他），自定义存储路径，目标字数（默认10万字），简介（500字限制）
- **项目设置**：可修改书名、作者、类型、简介
- **项目删除**：二次确认后删除
- **字数统计**：从章节存储中计算每个项目的总字数，实时显示

### 2.6 章节管理（已完成）

**实现方式**：
- Zustand `chapterStore` 管理章节状态
- SQLite `chapters` 表持久化章节数据
- 编辑器左侧栏显示章节列表

**具体功能**：
- 章节列表：显示章节标题，高亮当前章节
- 新建章节：输入标题后创建
- 删除章节：删除指定章节
- 章节排序：通过 `order` 字段控制顺序
- 自动保存：30秒间隔自动保存到磁盘（通过 `useAutoSave` hook）

### 2.7 富文本编辑器（已完成）

**实现方式**：
- 基于 TipTap v3（ProseMirror）构建
- 在 `src/components/editor/Editor.tsx` 中封装
- 使用 `forwardRef` + `useImperativeHandle` 暴露 editor 实例

**具体功能**：
- **工具栏**：加粗、斜体、删除线、H1/H2/H3 标题、无序/有序列表、引用、代码块、分隔线、撤销/重做
- **编辑区**：使用衬线字体排版，阅读宽度可控（42字符）
- **底部状态栏**：本章字数、全书字数、保存按钮、自动保存状态
- **占位符**：空章节显示"开始写作 xxx..."

### 2.8 AI 写作助手（已完成）

**实现方式**：
- `src/services/aiService.ts` — 统一 AI 接口抽象层
- `src/services/ollama.ts` — Ollama 原生 API 实现
- `src/services/openaiCompat.ts` — OpenAI 兼容协议实现
- `src/components/ai/AIPanel.tsx` — AI 聊天面板 UI

**架构设计**：
```
BackendType: "ollama" | "llamacpp" | "vllm" | "lmstudio" | "openai-compat"
     ↓
createAIService(config) → 根据 backend 类型路由:
  - "ollama" → OllamaService (原生 Ollama API)
  - 其他 → OpenAICompatService (OpenAI Chat Completions API)
     ↓
aiService.chatStream(messages, options, onChunk) → 流式输出
```

**具体功能**：
- **4 种快捷操作**：
  - 续写（continuation）：取章节最后 500 字作为上下文，追加到正文
  - 润色（polish）：需选中文本，替换选中内容
  - 扩写（expand）：需选中文本，替换选中内容
  - 缩写（compress）：需选中文本，替换选中内容
- **自由对话**：用户可输入任意需求与 AI 对话
- **流式响应**：AI 回复实时逐字显示
- **上下文管理**：保留最近 6 条对话历史
- **应用到正文**：AI 生成的内容可一键应用到编辑器（追加或替换选中区域）
- **连接状态指示**：绿点=已连接、红点=未连接、黄点=检测中
- **10 种系统提示词**：writer/continuation/polish/expand/compress/character/worldbuilding/plot/dialogue/consistency/reader

**AI 参数配置**：
| 参数 | 范围 | 默认值 | 说明 |
|------|------|--------|------|
| Temperature | 0-2 | 0.7 | 控制输出随机性 |
| Top P | 0-1 | 0.9 | 核采样阈值 |
| Top K | 1-100 | 40 | 候选词数量（仅 Ollama） |
| 重复惩罚 | 1-2 | 1.1 | 惩罚已出现过的词 |
| 最大 Token | 256-8192 | 2048 | 单次生成的最大长度 |
| 关闭思考 | boolean | true | 关闭模型内部推理（仅 Ollama） |

### 2.9 大纲管理（已完成）

**实现方式**：
- Zustand `outlineStore` 管理大纲状态
- 三级嵌套数据模型：Volume[] → OutlineChapter[] → Scene[]
- SQLite `outline_volumes/outline_chapters/outline_scenes` 表持久化

**具体功能**：
- **三级树状结构**：卷 → 章 → 场景
- **卷管理**：展开/折叠，显示章节数，支持添加/删除
- **章管理**：展开/折叠，显示状态标签（草稿/写作中/已完成），支持添加场景/删除
- **场景管理**：显示序号，支持删除
- **即时创建**：输入名称回车确认，Esc 取消

### 2.10 人物系统（已完成）

**实现方式**：
- Zustand `characterStore` 管理人物状态
- SQLite `characters` 表持久化，15+ 字段

**具体功能**：
- **人物列表**：卡片网格布局，首字头像，显示姓名/性别/年龄/别名数/性格/标签
- **人物编辑**：12 个字段，所有字段实时保存
  - 姓名、别名（逗号分隔）
  - 性别（男/女/其他）、年龄
  - 外貌描写、性格特点、背景故事
  - 目标动机、冲突矛盾、人际关系
  - 能力特长、弱点缺陷
  - 标签（逗号分隔）、备注

### 2.11 世界观系统（已完成）

**实现方式**：
- Zustand `worldviewStore` 管理世界观状态
- SQLite `worldview_items` 表持久化

**具体功能**：
- **10 种类型**：地点、组织、物品、事件、规则、种族、魔法体系、科技体系、历史事件、其他
- **类型筛选**：SegmentedControl 按类型筛选，显示各类型计数
- **设定编辑**：名称、类型、简要描述、详细信息、关联关系、备注

### 2.12 伏笔系统（已完成）

**实现方式**：
- Zustand `foreshadowingStore` 管理伏笔状态
- SQLite `foreshadowing_items` 表持久化

**具体功能**：
- **3 种状态**：已埋设 (planted)、已回收 (revealed)、已废弃 (abandoned)
- **统计面板**：4 个 StatCard 显示各状态数量
- **状态筛选**：SegmentedControl 按状态筛选
- **重要程度**：低/中/高，高重要度显示红色"重要"标签
- **伏笔编辑**：8 个字段（名称/状态/重要度/描述/埋设章节/回收章节/埋设内容/回收内容/关联人物/备注）

### 2.13 导出功能（已完成）

**实现方式**：
- `src/services/export.ts` — 导出服务
- 支持 4 种格式：TXT / Markdown / HTML / DOCX

**具体功能**：
- **TXT**：纯文本导出，自动去除 HTML 标签，保留标题和作者信息
- **Markdown**：保留 HTML 内容，添加标题和作者元信息
- **HTML**：生成完整 HTML 页面，包含 CSS 样式（中文字体、行高、缩进）
- **DOCX**：使用 `docx` 库生成 Word 文档，包含标题、作者、段落格式
- **导出范围**：单章导出 + 批量导出整个项目（每章节一个文件）

### 2.14 备份与恢复（已完成）

**实现方式**：
- `src/services/backup.ts` — 备份服务
- 导出为 JSON 文件，导入时覆盖当前数据

**具体功能**：
- **全量备份**：导出所有数据（项目、章节、人物、世界观、大纲、伏笔）
- **项目备份**：仅导出指定项目的相关数据
- **导入恢复**：从 JSON 备份文件恢复，覆盖当前所有数据，有警告提示

### 2.15 搜索功能（已完成）

**实现方式**：
- `src/components/search/SearchDialog.tsx` — 全局搜索对话框
- Ctrl+K 触发

**具体功能**：
- **搜索范围**：当前项目内的 4 种内容类型
  - 章节：搜索章节标题和正文内容（去除 HTML 标签后搜索）
  - 人物：搜索姓名、性格、背景
  - 世界观设定：搜索名称、描述、详情
  - 伏笔：搜索名称、描述、埋设内容、备注
- **搜索结果**：最多显示 20 条，每条显示类型标签、标题、内容摘要
- **搜索结果导航**：点击结果跳转到对应页面

### 2.16 快捷键系统（已完成）

**实现方式**：
- `src/hooks/useKeyboardShortcuts.ts` — 全局键盘快捷键 hook
- 支持 Ctrl/Shift/Alt 组合键，兼容 Mac Command 键

**已注册的快捷键**：
| 快捷键 | 功能 |
|--------|------|
| Ctrl+1 | 跳转编辑器 |
| Ctrl+2 | 跳转大纲 |
| Ctrl+3 | 跳转人物 |
| Ctrl+4 | 跳转世界观 |
| Ctrl+5 | 跳转伏笔 |
| Ctrl+6 | 跳转设置 |
| Ctrl+K | 打开搜索 |
| ? | 显示快捷键帮助 |

### 2.17 自动保存（已完成）

**实现方式**：
- `src/hooks/useAutoSave.ts` — 自动保存 hook
- 使用 `useRef` 存储数据和上次保存的 JSON 字符串
- 定时器对比 `JSON.stringify(data)` 与上次保存值，不同才触发保存

**具体功能**：
- 默认 30 秒间隔，可在设置中调整（10~300 秒）
- 支持开启/关闭自动保存
- 手动保存按钮（`saveNow()` 方法）

### 2.18 设置页面（已完成）

**实现方式**：
- `src/pages/SettingsPage.tsx` — 设置页面
- `src/stores/settingsStore.ts` — 设置状态管理

**具体功能**：
- **外观设置**：ThemeToggle 组件（模式/强调色/色调/边框圆角）
- **编辑器设置**：
  - 自动保存开关
  - 自动保存间隔（10~300 秒）
  - 编辑器宽度（30~60 字符）
- **AI 模型配置**：
  - 推理后端选择（5 种预设）
  - 模型端点 URL
  - API Key（非 Ollama 时显示）
  - 模型名称（支持手动输入 + 自动检测可用模型列表）
  - 连接检测按钮
  - 参数滑块（Temperature/Top P/Top K/重复惩罚）
  - 最大 Token 数（256-8192）
  - 关闭思考开关（仅 Ollama）
- **数据备份与恢复**：导出全部数据/导出当前项目/导入备份
- **关于**：版本号、简介、开发者链接（Jack）、项目地址

---

## 三、数据持久化架构

### 3.1 三层存储架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     数据存储三层架构                              │
├──────────────┬──────────────────┬───────────────────────────────┤
│  localStorage │  Zustand+persist │  SQLite (tauri-plugin-sql)   │
│  (浏览器原生)  │  (localStorage)  │  (文件系统 .db)              │
├──────────────┼──────────────────┼───────────────────────────────┤
│  currentUserId │ projects        │  users                       │
│  (仅存用户ID)  │ chapters        │  projects                    │
│               │ characters      │  chapters                    │
│               │ worldviewItems  │  characters                  │
│               │ outlineVolumes  │  worldview_items             │
│               │ foreshadowing   │  outline_volumes/chapters/   │
│               │ settings        │   scenes                     │
│               │ theme           │  foreshadowing_items         │
│               │                 │  settings                    │
└──────────────┴──────────────────┴───────────────────────────────┘
```

### 3.2 数据流转

1. **用户数据**：存储在 SQLite 的 `users` 表中，`userStore` 直接通过 `userDb`（SQLite）操作，登录状态通过 localStorage 保持
2. **业务数据**：实际运行时存储在 Zustand 的 `persist` 中间件中，底层是 localStorage
3. **设置数据**：AI 参数和编辑器设置存储在 Zustand `settingsStore`（persist → localStorage）
4. **文件系统**：`fileStorage.ts` 通过 Tauri FS 插件操作磁盘文件，但当前 Store 层并未主动调用

---

## 四、技术债务与待解决问题

### 4.1 双轨存储未打通（严重）

**问题描述**：
- SQLite 数据库层（`database.ts`）已实现完整的 8 张表 CRUD
- 但上层的 Zustand Store 并未依赖数据库层
- 数据实际存储在 localStorage，SQLite 表虽已建好但未被使用

**影响**：
- 数据无法跨设备同步
- 无法利用 SQLite 的事务、索引、查询能力
- 备份/恢复只操作 localStorage，不涉及 SQLite

### 4.2 Rust 后端空壳（严重）

**问题描述**：
- `src-tauri/src/lib.rs` 仅注册了 4 个 Tauri 插件
- 没有任何自定义 Tauri command
- PRD 设计的 Rust 职责（文件监听、模型代理、向量索引、插件沙箱）均未实现

**影响**：
- AI 请求由前端直接 `fetch` 调用本地模型，绕过了 Rust 层
- 无法实现文件监听、后台任务等需要 Rust 端的功能

### 4.3 文件系统未集成

**问题描述**：
- `fileStorage.ts` 实现了完整的文件系统 API（创建项目目录、保存/加载章节等）
- 但 Store 层并未调用这些 API，数据仍存储在 localStorage

**影响**：
- 项目文件未实际写入磁盘
- 无法实现 PRD 设计的 `.novel/` 目录结构

### 4.4 用户密码明文存储

**问题描述**：
- 用户密码直接存储在 SQLite 的 `users` 表中，没有进行 hash 处理

**影响**：
- 存在安全隐患，数据库泄露会导致密码暴露

### 4.5 CSP 设置为 null

**问题描述**：
- `tauri.conf.json` 中 CSP 设置为 null，完全禁用 Content Security Policy

**影响**：
- 开发阶段可接受，但生产环境需要收紧

---

## 五、PRD 偏离点分析

### 5.1 版本升级（正向偏离）

| 项目 | PRD 设计 | 当前实现 | 说明 |
|------|----------|----------|------|
| React | React 18 | React 19.1 | 已升级到最新版本 |
| TypeScript | TS 5 | TS 6.0 | 已升级到最新版本 |
| 路由 | React Router v6 | React Router v7.18 | 已升级到最新版本 |
| Vite | 通用 | Vite 8.0 | 已升级到最新版本 |

### 5.2 架构偏离

| 项目 | PRD 设计 | 当前实现 | 偏离程度 |
|------|----------|----------|----------|
| **数据持久化** | SQLite 为主存储（通过 Rust 后端） | Zustand + localStorage 为主，SQLite 已建表但未集成 | 严重偏离 |
| **Rust 后端** | 文件读写、项目扫描、模型调用代理、向量索引、插件沙箱 | 仅注册插件，无自定义 command | 严重偏离 |
| **AI 请求路由** | 由 Rust 后端转发至本地模型 | 前端直接 `fetch` 调用本地模型 | 偏离 |
| **文件系统集成** | `.novel/` 目录结构是核心 | `fileStorage.ts` 实现了但未集成到 UI | 部分偏离 |
| **向量索引** | sqlite-vec 用于语义检索 | 未实现 | 未开始 |
| **Embedding** | 本地 embedding 模型 | 未实现 | 未开始 |

### 5.3 功能偏离

| 项目 | PRD 设计 | 当前实现 | 说明 |
|------|----------|----------|------|
| **用户系统** | PRD 未明确提及多用户 | 实现了注册/登录/多用户 | 超出 PRD |
| **模板项目** | 内置玄幻/都市/悬疑/科幻等模板 | 未实现 | 缺失 |
| **项目导入** | 支持 TXT/DOCX/EPUB 导入分章 | 未实现 | 缺失 |
| **记忆系统** | sqlite-vec 向量索引、长期记忆 | 未实现 | 缺失 |
| **一致性检查** | 人物/外貌/性格/口吻/能力等一致性检查 | 未实现 | 缺失 |
| **文风系统** | 文风定义/来源/训练/应用 | 未实现 | 缺失 |
| **版本对比** | 并排对比、行内对比、差异高亮 | 未实现 | 缺失 |
| **提示词管理** | 内置提示词、自定义提示词、版本管理 | 仅硬编码 10 个提示词 | 部分实现 |
| **工作流** | 可视化工作流、节点、连线 | 未实现 | 缺失 |
| **多模型对比** | 并排对比、评分、投票 | 未实现 | 缺失 |
| **编辑器高级功能** | 查找替换、批注、修订、番茄钟等 | 仅基础编辑功能 | 大量缺失 |
| **插件系统** | 插件安装/卸载/权限/沙箱 | 未实现 | 缺失 |
| **统计分析** | 写作日历热力图、字数趋势等 | 仅基础字数统计 | 大量缺失 |
| **国际化** | 中文/英文/语言包 | 未实现 | 缺失 |
| **新手引导** | 欢迎页、功能导览、教程 | 未实现 | 缺失 |

### 5.4 项目目录结构差异

PRD 设计的每个项目的 `.novel/` 目录下有丰富的子目录：
```
.novel/
  project.json
  settings/ worldview/ outline/ memory/
  prompts/ workflows/ versions/ logs/
  cache/ plugins/ assets/
  vectors.db  index.db
```

当前实现中 `fileStorage.ts` 创建了目录骨架，但所有数据实际存储在 Zustand 的 localStorage 中，未写入磁盘。

---

## 六、待完成功能清单（按优先级）

### 6.1 MVP 阶段（阶段一）缺失

- [ ] 模板项目创建（玄幻/都市/悬疑/科幻模板）
- [ ] 从导入创建（TXT/DOCX/EPUB 导入分章）
- [ ] 文件系统集成（项目实际存储到磁盘而非 localStorage）
- [ ] SQLite 数据库与 Store 的数据同步
- [ ] Rust 后端 command（项目扫描、文件监听等）
- [ ] EPUB 导出

### 6.2 核心增强（阶段二）完全缺失

- [ ] 记忆系统（sqlite-vec 向量索引）
- [ ] 一致性检查
- [ ] 文风系统
- [ ] 版本对比
- [ ] 提示词管理（当前为硬编码 10 个提示词）
- [ ] 工作流
- [ ] 多模型对比

### 6.3 UI/编辑器缺失

- [ ] 编辑器功能：查找替换、正则查找、拼写检查、批注、高亮、书签、折叠
- [ ] 写作模式：目标写作、番茄钟、小黑屋模式
- [ ] 修订模式
- [ ] 多标签页
- [ ] 可拖拽面板
- [ ] 状态栏信息（字数统计、模型状态等）
- [ ] AI 操作历史（当前对话不持久化）

### 6.4 其他缺失

- [ ] 插件系统
- [ ] 搜索与全局检索（SearchDialog 组件存在但功能有限）
- [ ] 统计与分析（写作日历热力图、字数趋势等）
- [ ] 国际化
- [ ] 新手引导
- [ ] 日志系统
- [ ] 崩溃恢复
- [ ] 项目导入冲突处理
- [ ] 批量操作
- [ ] 可选 AES-256 加密

---

## 七、Git 提交记录

| Commit | Message |
|--------|---------|
| `f0a44aa` | fix(ui): 修复下拉选择框样式并增强设置功能 |
| `939a35f` | feat(bookshelf): 书架页添加字数统计显示 |
| `d9c7821` | feat(website): add dark mode toggle with system preference detection |
| `710bb78` | feat(website): add tech stack, download section, and footer |
| `5a48ee2` | feat(website): add features showcase section |
| `2cb27ee` | feat(website): add hero section with download CTA |
| `2a59be7` | feat(website): add header navigation component |
| `29439a3` | feat(website): migrate design tokens and global styles from desktop app |
| `257cc0c` | feat(website): initialize React + TypeScript project scaffold |
| `dfa3e73` | feat(theme): 丰富主题系统并优化侧边栏切换 |
| `74b34e7` | feat(settings): 设置页面增强 |
| `8258358` | fix(settings): 修复设置页面因 backend 未定义导致崩溃的问题 |
| `7c97e5b` | feat(ai): 支持多推理框架（Ollama + OpenAI 兼容） |
| `8cba1b7` | refactor(editor): 改进编辑器保存功能 |
| `557a8ff` | fix(storage): 修复文件夹结构生成问题 |
| `1fbdc0a` | fix(storage): 编辑器内容自动保存到磁盘 |
| `6959a40` | fix(storage): 修复存储路径未生效的问题 |
| `0ac1113` | chore: 清理旧的 AI 组件和路由 |
| `e8a1770` | refactor: 集成 AI 面板到编辑器页面并移除独立页面 |
| `2d3fb71` | refactor(editor): 重构编辑器支持 AI 面板集成 |
| `098df15` | feat(ai): 创建 AI 聊天面板组件 |
| `c602aa7` | fix(editor): 修复TipTap v3编辑器无法使用的问题 |
| `ead2871` | feat(ai): 添加关闭思考功能 |
| `3f0afb7` | feat(settings): 关于页面改为显示GitHub地址 |
| `cc2349e` | fix(settings): 检测连接失败时添加反馈提示 |
| `f6f789f` | docs(settings): 为AI参数添加详细说明 |
| `f3a97d5` | fix(dialog): 修复弹窗先出现在左上角再闪到中间的问题 |
| `4023e90` | fix(input): 修复目标字数输入框验证错误 |
| `6077ec0` | refactor(ui): 统一书架页和新建作品页的UI风格 |
| `c31dd65` | fix(auth): 修复登录注册功能 |
| `4e934cd` | feat(auth): 删除快速开始功能 |
| `6448dfc` | fix(auth): 修复功能卡片位置问题 |
| `ff7f0e2` | fix(auth): 修复登录注册动画和布局问题 |
| `bff5743` | feat(auth): 登录注册页面添加动画效果 |
| `b73259a` | feat(auth): 完善登录注册功能 |
| `dbb8901` | docs(icon): 添加应用图标替换说明 |
| `3d1792d` | feat(characters): 扩展人物字段 |
| `6bd5707` | feat(project): 新建项目支持自定义存储路径 |
| `5c1a5cd` | feat(project): 新建项目改为独立页面 |
| `afbd272` | feat(auth): 添加本地用户系统 |
| `61aa208` | feat(db): 迁移到 SQLite 数据库 |

---

## 八、总结

### 8.1 当前项目状态

项目处于 **MVP 早期阶段**，已经搭建了完整的前端架构骨架：

1. **前端架构完整**：React 19 + TypeScript + Vite + Tailwind + Zustand，路由、Store、组件、Hooks、Services 层次清晰
2. **AI 集成可用**：支持 Ollama + OpenAI 兼容协议，流式输出，5 种后端预设
3. **UI 组件体系**：自定义 Dialog/Button/Badge/Card/Input 等 UI 组件，主题系统 4 维定制
4. **数据模型完整**：8 张 SQLite 表 + 9 个 Zustand Store，覆盖项目/章节/人物/世界观/大纲/伏笔/设置

### 8.2 关键技术债务

1. **双轨存储未打通**：SQLite 数据库层已实现但 Store 层未调用，数据实际存储在 localStorage
2. **Rust 后端空壳**：仅注册插件，无任何自定义 command，PRD 设计的 Rust 职责均未实现
3. **AI 请求绕过 Rust**：前端直接 fetch 本地模型服务，而非通过 Rust 代理
4. **文件系统未集成**：`fileStorage.ts` API 完整但未接入 UI 流程
5. **无向量/记忆系统**：PRD 核心差异化功能 sqlite-vec 完全未开始

### 8.3 下一步建议

1. **优先打通存储层**：将 Zustand Store 的数据同步到 SQLite，实现真正的本地持久化
2. **实现 Rust 后端**：添加自定义 command，实现文件监听、模型代理等功能
3. **集成文件系统**：将项目数据实际写入磁盘，实现 PRD 设计的 `.novel/` 目录结构
4. **实现记忆系统**：引入 sqlite-vec，实现向量索引和语义检索
5. **完善编辑器功能**：添加查找替换、批注、修订等高级功能

---

*报告完成时间：2026-09-13*
